from centos:7
run yum install bc psmisc openssh-clients pciutils procps-ng less wget unzip bash-completion iproute iputils net-tools -y && yum clean all
cmd date; echo 123; trap 'echo caught the TERM signal, exit 1; exit 1' SIGTERM; mkfifo x; read < x
