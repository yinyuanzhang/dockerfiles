FROM maliciousgenius/root
# здесь должны собираться и тестироватьсться собственные сборки компиляторов (лежат в /space)
# в теории нужно реализовать 2 стека языков LLVM/Verilog и λ+x для установки в корень и метапроцессор
# образует контекст сложности

# -

# подпись
LABEL MAINTAINER="Dmitriy Detkov"
LABEL MAIL="maliciousgenius@gmail.com"
LABEL TEL="+79604565686"
