# This container will exit as soon as it is started
# its purpose is to test that docker is able to run it with the project folder mounted,
# it will be cleaned up as soon as it exists

FROM alpine:latest
CMD ["/bin/kill", "-9", "1"]